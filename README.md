# webhook-repo
Flask-based GitHub webhook receiver that:
- Captures GitHub events (Push, Pull Request, Merge)
- Stores minimal event data in MongoDB
- Displays recent activity on a simple UI

More details will be added after implementation.
